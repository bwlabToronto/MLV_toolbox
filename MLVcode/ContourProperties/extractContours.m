function pixLD = extractContours(photo, parameters)
% pixLD = extractContours(photo, parameters)
%   Extracts the contours in color photograph given by photo and returns a
%   pixel image with contours in pixLD.
% 
% Input: 
%   photo: a color photograph in UINT8 or double format.
%   parameters; TBD
%
% Output:
%   pixLD: grayscale image of the same size as photo with contours.

